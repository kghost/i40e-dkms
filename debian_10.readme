we have added a patch for debian 10. Kernels prior to 5.1 do not have the auxiliary bus driver.
So we have to build it for debian 10
 * patch -p1 < debian_10.patch

This patch compiles/install the intel auxiliary kernel module
